# Linked List

when to use Linked List and ArrayList

here's some properties

ArrayList:

insert: o(n);
search: o(1);

LinkedList:
insert: o(1);
search: o(n);

so, for searching opertion we use ArrayList but if we want maniplation to fast so, we use LinkedList

maniplation means adding, deleting or updating.

LinkedList:

Variable size
non - contigous memory
insert in o(1)
search in o(n)

LinkedList structure
data|node


3 Types of LinkedList

singley LinkedList
Doubley LinkedList
circular LinkedList